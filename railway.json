{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && DJANGO_SETTINGS_MODULE=bloodbankmanagement.settings_railway gunicorn bloodbankmanagement.wsgi:application --bind 0.0.0.0:$PORT",
    "numReplicas": 1,
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
